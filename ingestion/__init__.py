"""
Purpose:

* Marks ingestion as a package for loading and chunking modules.

Inputs:

* No direct runtime inputs.

Outputs:

* Importable ingestion module namespace.

Used in:

* Ingestion pipeline and API auto-ingestion imports.
"""
